<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="net.softsociety.testboot.dao.StudyCourseDAO">

<!-- 학습페이지에서 입문편 리스트 조회-->
<select id="" parameterType="String" resultType="TestLessonVO">
	select * 
	from  test_lesson
	where GRADE = #{intro}	
</select>

</mapper>